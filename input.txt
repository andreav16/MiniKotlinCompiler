fun main() {
  val firstName = "John "
  val lastName = "Doe"
  val fullName = firstName + lastName
  println(fullName)

   val time = 20
  val greeting = if (time < 18) {
    "Good day."
  } else {
    "Good evening."
  }
  println(greeting)
}